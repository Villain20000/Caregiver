{
  "name": "carevibe-root",
  "version": "1.0.0",
  "description": "Root workspace for CareVibe Caregiver Platform (Angular frontend & Express backend)",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix frontend && npm install --prefix backend",
    "start": "npm run start:dev",
    "start:dev": "npm run dev --prefix backend & npm run start --prefix frontend",
    "build:all": "npm run build --prefix backend && npm run build --prefix frontend"
  }
}
