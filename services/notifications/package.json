{
  "//": "services/notifications — real-time alert engine microservice. Consumes vitals + appointment events from Kafka, checks clinical thresholds, persists alerts to Postgres, and emits `alert.dispatched` events for the API gateway to fan out to connected frontend clients via Socket.io.",
  "name": "@caregiver/notifications",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/main.ts",
  "scripts": {
    "build": "tsc -b",
    "start": "node dist/main.js",
    "dev": "tsx watch src/main.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "//dependencies": "NestJS 10 on Express as the DI container + lifecycle host, KafkaJS (via @caregiver/kafka) for event consumption/production, Drizzle ORM (via @caregiver/db) for alert persistence, @caregiver/contracts for typed event payloads, reflect-metadata for NestJS DI.",
  "dependencies": {
    "@caregiver/contracts": "0.1.0",
    "@caregiver/db": "0.1.0",
    "@caregiver/kafka": "0.1.0",
    "@nestjs/common": "^10.4.0",
    "@nestjs/core": "^10.4.0",
    "@nestjs/platform-express": "^10.4.0",
    "drizzle-orm": "^0.33.0",
    "kafkajs": "^2.2.4",
    "postgres": "^3.4.4",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "tsx": "^4.19.0",
    "typescript": "^5.5.4",
    "vitest": "^2.1.0"
  }
}
