{
  "//": "packages/ui — shared Angular standalone component library. Consumed by apps/web.",
  "name": "@caregiver/ui",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "//peerDependencies": "Angular is provided by apps/web, not bundled here. @caregiver/rbac is used by RoleBadge.",
  "peerDependencies": {
    "@angular/core": "^17.0.0",
    "@angular/common": "^17.0.0",
    "@caregiver/rbac": "^0.1.0",
    "rxjs": "^7.8.0"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
