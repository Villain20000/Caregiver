/**
 * packages/config/src/index.ts
 *
 * Shared configuration re-exports for the Caregiver monorepo.
 *
 * Phase 1: placeholder. Phase 2 will populate this with helpers for
 * loading environment variables, validating config schemas (zod), and
 * re-exporting shared ESLint/TS/Prettier configuration objects.
 */

/** Semantic version of the config package. */
export const CONFIG_VERSION = '0.1.0';
